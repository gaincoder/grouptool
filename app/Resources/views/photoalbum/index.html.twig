{% extends 'base.html.twig' %}
{% block content_row %}
    <div class="container container-fluid" id="home">
        <!-- HOME -->

        <!-- ABOUT & UPDATES -->
        <div class="row-fluid" id="about">
            <div class="span12">
                <h2 class="page-title" id="scroll_up"> Fotoalben</h2>
                <a href="{{ path('photoalbum_create') }}" class="btn btn-sm btn-success"><span class="glyphicon
                glyphicon-plus"></span> hinzufügen</a><br><br>
                <table class="table table-striped">
                    {% for photoalbum in photoalbums %}
                        <tr>
                            <td><a href="{{ path('photoalbum_view',{photoalbum:photoalbum.id}) }}">{{ photoalbum.name }}</a></td>
                            <td><a href="{{ path('photoalbum_edit',{photoalbum:photoalbum.id}) }}" class="btn btn-sm btn-warning"><span class="glyphicon glyphicon-pencil"></span></a> <a href="{{ path('photoalbum_delete',{photoalbum:photoalbum.id}) }}" class="btn btn-sm btn-danger"><span class="glyphicon glyphicon-trash"></span></a></td>
                        </tr>
                    {% endfor %}
                </table>
            </div>

        </div>


    </div>
{% endblock %}